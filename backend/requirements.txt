# FastAPI Backend Dependencies
# Note: ML dependencies (catboost, shap, pandas, numpy, scikit-learn)
# are already installed from the main project requirements.txt

fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
python-multipart==0.0.6
